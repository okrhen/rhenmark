import NotFound from 'components/not-found';
import { IPath } from 'interfaces/shared';
import React from 'react';

const Project = ({ path }: IPath) => {
  return <NotFound />;
};

export default Project;
