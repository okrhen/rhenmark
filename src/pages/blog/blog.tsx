import NotFound from 'components/not-found';
import { IPath } from 'interfaces/shared';
import React from 'react';

const Blog = ({ path }: IPath) => {
  return <NotFound />;
};

export default Blog;
